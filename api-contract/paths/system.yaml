/shutdown:
  post:
    tags:
      - System
    summary: Shutdown Server
    description: Initiate server shutdown
    operationId: shutdown
    responses:
      '200':
        description: Successful Response
        content:
          application/json:
            schema:
              $ref: '../schemas/common.yaml#/ShutdownResponse'
      '422':
        $ref: '../responses/errors.yaml#/ValidationError'